# @native-surface/design-plane

Vite plugin. Adds `/plane`: pan/zoom canvas of mocked app routes on `NativeSurface`.

Host files: `.native-surface/plane.tsx`, `.native-surface/wrapper.tsx`. Agent instructions: [SKILL.md](./SKILL.md).
