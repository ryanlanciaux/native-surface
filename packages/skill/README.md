# @native-surface/skill

Optional agent skill. Not required to run the playground.

```sh
npm i -D @native-surface/skill
# Pi: pi install npm:@native-surface/skill
```

Then: *“Make stories for all components and add my screens to the design plane.”*
